// Written by scripts/export-oss.ts. Default copilot provider is null;
// register your own provider via @stem/editor-oss/copilot to enable it.
export function initIntegratedCopilotProvider(_getApp: () => unknown): void {
    // no-op
}
