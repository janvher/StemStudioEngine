This directory contains WebAssembly for encoding compressed textures.

The source code is from the following GitHub project: https://github.com/gz65555/ktx2-encoder

There are two files:

* basis_encoder.js
* basis_encoder.wasm

We also import the npm package for this repository. If we update that npm package we should also update these files.