/**
 * Local-Node adapter — placeholder.
 *
 * This file exists so the directory structure mirrors `remote-go/`. The
 * implementation will fill in once `network/src/api.ts` defines the
 * canonical interface that both adapters satisfy.
 *
 * See `client/packages/network/src/adapters/local-node/README.md` for the
 * roadmap.
 */

export const LOCAL_NODE_ADAPTER_PLACEHOLDER = true;
